class CanvasSystem extends System {
  constructor() {
    super();
  }

  _UpdateEntitie(entity) {
    // if (entity._size.aspectRatio == "fullscreen") {
    //   entity._width = MTDEF.Window.width;
    //   entity._height = MTDEF.Window.height;
    // }
  }

  _ExecuteEntitie(entity) {
    // const canvas = entity.html,
    //   style = canvas;
    // if (entity._fullscreen) {
    //   if (entity._width != canvas.width) {
    //     canvas.width = entity.width;
    //   }
    //   if (entity._height != canvas.height) {
    //     canvas.height = entity.height;
    //   }
    //   style.position = "fixed";
    //   style.left = entity.pos.x;
    //   style.top = entity.pos.y;
    // } else {
    //   style.marginLeft = "auto";
    //   style.marginRight = "auto";
    //   style.display = "block";
    //   entity._SetPosistion();
    // }
    // if (entity._backgroundColor == "EMPTY") {
    //   context.clearRect(0, 0, html.width, html.height);
    // } else {
    //   context.fillStyle = this._backgroundColor;
    //   context.fillRect(0, 0, html.width, html.height);
    // }
  }
}
